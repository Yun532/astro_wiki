# Starlight setup notes

建议 Claude Code 初始化：

```powershell
cd site
npm create astro@latest . -- --template starlight
npm install
```

本地预览：`npm.cmd run dev --prefix site`
构建：`npm.cmd run build --prefix site`
